# Railway Environment Variables
NODE_ENV=production
PORT=10000

# Database Configuration (Railway will provide these)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=todo_db
DB_USER=todo_user
DB_PASSWORD=your_password_here

# Frontend API URL (update with your Railway backend URL)
VITE_API_URL=http://localhost:10000/api
